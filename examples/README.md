# 示例实验包

`sample_experiment_package` 是 `.pexp` 内容包的解包形态。打包命令：

```sh
cd examples/sample_experiment_package
zip -r ../drug_ce_low_eof_variant.pexp manifest.json lesson.md report_template.md assets
```

生成的 `drug_ce_low_eof_variant.pexp` 可在应用的“实验包管理”中导入。
